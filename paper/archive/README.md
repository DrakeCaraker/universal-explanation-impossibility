# Archive

v1_original: the pre-restructure monograph. restructure_monograph_v2.py: the
deterministic generator that produced the tiered v2 from it (source paths need
adjusting to reproduce). The canonical, directly-edited monograph now lives at
paper/universal_impossibility_monograph.tex, protected by `make ci`.
